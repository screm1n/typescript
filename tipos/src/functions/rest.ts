function totalVendas(venda1: number, venda2: number, venda3: number): number {
    const total = venda1 + venda2 + venda3 + venda4;

    console.log(total);

    return total
}

totalVendas(10, 30, 50, 10)