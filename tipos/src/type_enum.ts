// type ENUM (enumerar)

enum DesignColors {
    white = "#FFFFFF",
    black = "#000000"
}

// console.log(DesignColors.white)

enum StatusPermission {
    ADMIN, 
    USER
}

console.log(StatusPermission.ADMIN)